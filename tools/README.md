# Tools

Deterministic Python scripts live here.

Tools should:
- Take explicit inputs.
- Do one execution task well.
- Read secrets from `.env`.
- Produce outputs in `.tmp/` or external services as required by a workflow.
